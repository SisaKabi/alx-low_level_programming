#include <stdio.h>
#include <string.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char a[55];
	
	strcpy(a,'"Programming is like building a multilingual puzzle"');
	puts(a);

	return (0);
}
